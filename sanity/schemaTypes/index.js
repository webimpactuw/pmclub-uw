import eventInfo from '../schemas/eventInfo'
import duration from '../schemas/objects/duration'
import dayAndDate from '../schemas/objects/dayAndDate'

export const schemaTypes = [eventInfo, duration, dayAndDate]
